mixin pratchettHeader
  include compass.pug
  include icons.pug
  .pratchett-header
    +bigWave(1)
    +smallWave(1)
    +reversiveSmallWave(2)
    +reversiveBigWave(2)
    h1.pratchett-header__strip.text-center
      img(src=imgUrl+'title.svg' width="1190" height="216")
    .pratchett-header__world.text-center
      .turtle
        +compass
        .flags
          for block in flags
            .flags__block
              -var i = 1
              while i <= block.length
                .flag(data-index=i data-cycle=block.id)
                  svg.flag-icon: use(xlink:href="#flag-icon")
                  -i++
      +bookCard

mixin bookCard
  .book-card
    svg.close: use(xlink:href="#close-icon")
    a.book-card__img(href="")
      img(src="" alt="")
    .book-card__info
      p
        a.book-card__title(href="")
      p.book-card__author
      .book-rating(data-rating="1")
        .book-rating__title Рейтинг: #[span.count]
        .book-rating__vote
        .book-rating__stars
          .book-rating__star
          .book-rating__star
          .book-rating__star
          .book-rating__star
          .book-rating__star
          .book-rating__star
          .book-rating__star
          .book-rating__star
          .book-rating__star
          .book-rating__star
      p.book-card__year Год издания: #[span.year]
      p.book-card__publisher Издательство: #[span.publisher]
      p.book-card__price Цена: #[b #[span.price]&#8399;]
      p.book-card__annotation
      .book-card__button
        button(type="button" data-element="") В корзину